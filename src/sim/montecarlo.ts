// Monte Carlo probability simulator. Runnable via `npm run sim` (tsx).
// Real implementation lands in Step 3, once hand evaluation exists.

function main(): void {
  console.log('Simulatore Monte Carlo — placeholder (Step 3).')
}

main()
